# Kopiraj u `.env` i popuni.
#
# ListenNotes API ključ — koristi se samo za popis epizoda podcast-emisija
# (/emisije/<slug> i /api/listennotes/<id>). Čita se u runtimeu, pa build i
# dev server rade i bez njega — te emisije tada jednostavno nemaju popis epizoda.
LISTENNOTES_API_KEY=
